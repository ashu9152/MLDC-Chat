<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/image_to_View"
        android:layout_width="56dp"
        android:layout_height="53dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        android:adjustViewBounds="true"
        android:src="@drawable/default_avatar"
        android:clickable="true"
        android:focusable="true"
        android:scaleType="centerCrop"
        app:shapeAppearanceOverlay="@style/RoundedImageStyle"
        app:layout_constraintTop_toTopOf="parent"
         />

    <TextView
        android:id="@+id/text_to_View"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:background="@drawable/text_input_bg"
        android:maxWidth="240dp"
        android:padding="16dp"
        android:text="This is my message is to long to fit in one line so we are using max dp to make it more stable"
        app:layout_constraintEnd_toStartOf="@+id/image_to_View"
        app:layout_constraintTop_toTopOf="@+id/image_to_View" />
</androidx.constraintlayout.widget.ConstraintLayout>